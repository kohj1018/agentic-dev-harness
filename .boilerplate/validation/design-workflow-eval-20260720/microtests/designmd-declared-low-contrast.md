---
name: Low Contrast Fixture
colors:
  primary: "#777777"
  surface: "#ffffff"
typography:
  body:
    fontFamily: Arial
    fontSize: 16px
    fontWeight: 400
    lineHeight: 1.5
components:
  muted-copy:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.primary}"
---

## Overview

Fixture proving how the official linter reports a declared low-contrast component pair.

## Colors

Muted copy is deliberately below the 4.5:1 text threshold.

## Typography

Body copy uses the declared body token.

## Components

Muted copy uses the declared foreground and surface pair.
