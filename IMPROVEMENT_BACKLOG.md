# 개선 후보 정리 — 실제로 돌려보고 찾은 문제들

이 문서는 **이 저장소(보일러플레이트)를 실제로 한 번 돌려본 실험에서 찾은 문제들**을, 처음 보는 사람도 이해할 수 있게 풀어 쓴 것입니다. 쭉 읽으면 각 문제가 무엇이고, 왜 문제이며, 어떻게 고칠 수 있고, 고치면 뭐가 좋고 나쁜지까지 파악해서 "고칠지 말지"를 직접 결정할 수 있습니다.

---

## 0. 30초 요약

- **이 저장소가 뭐냐**: "AI가 소프트웨어를 만들 때 이런 순서·규칙으로 만들어라"를 문서와 스킬로 정해둔 **틀(보일러플레이트)**입니다. 사람이 규칙을 정해두면 AI가 그대로 따라 만듭니다.
- **무슨 실험을 했나**: 그 틀이 진짜 잘 작동하는지 확인하려고, **간단한 할 일(To-Do) 웹앱 하나("QuickTodo")를 그 틀 그대로 처음부터 끝까지 만들어봤습니다.** (이렇게 "자기 제품을 직접 써보며 검증"하는 걸 **dogfood(도그푸드)**라고 부릅니다.)
- **결과**: 앱은 잘 완성됐고, 그 과정에서 **틀 자체의 개선점 6가지**를 찾았습니다. 이 문서가 그 6가지 목록입니다.
- **가장 중요한 발견**: 이 틀의 마지막 품질 검사가 실제로 **다른 검사가 다 놓친 결함을 잡아냈습니다.** 즉 틀의 안전장치가 잘 작동한다는 게 증명됐고, 동시에 "그 결함을 더 일찍 잡을 수 있었다"는 개선점도 나왔습니다(아래 1번 문제).

---

## 1. 먼저 알아야 할 배경 (용어 5개)

문제들을 이해하려면 이 다섯 가지만 알면 됩니다.

1. **작업 흐름(lifecycle)** — AI가 소프트웨어를 만드는 정해진 순서입니다:
   *아이디어 발굴 → 기획 → 기술 선택 → 디자인 → 기능/작업 계획 → 구현 → 검증 → 마감 → 최종 "졸업" 판정.*
   각 단계마다 "스킬"이라는 작업 안내서가 있습니다(예: 계획 스킬, 검증 스킬).

2. **인수 기준(AC)** — "이 작업이 끝났다고 인정하려면 만족해야 하는 구체적 조건"입니다.
   예: *"엔터를 치면 입력한 할 일이 목록 맨 위에 나타난다."* 이 조건들을 테스트로 확인합니다.

3. **프로토타입** — 실제 코드를 짜기 전에 **화면이 어떻게 생기고 어떻게 동작할지 미리 만든 시안(HTML)**입니다. 승인되면 "이대로 만들어라"의 기준이 됩니다.

4. **경험 게이트(스크린샷 대조 검사)** — 앱을 다 만든 뒤, **실제로 만든 화면을 캡처해서 승인된 프로토타입과 나란히 비교**하는 마지막 검사입니다. "약속한 대로 만들었나?"를 사람 눈+AI로 확인합니다.

5. **ADR** — "이런 결정을 이런 이유로 내렸다"를 적어둔 **결정 기록 문서**입니다. 이 틀의 규칙들이 전부 ADR 번호로 박혀 있습니다(예: ADR-056 = 화면 경험을 어떻게 약속하고 검증하는지에 대한 규칙).

> 이 문서를 어떻게 쓰나: 각 문제마다 맨 아래에 **"내 추천"과 "당신의 결정" 칸**이 있습니다. 읽고 나서 채택/보류/기각을 정하면 됩니다. 나중에 이걸 바탕으로 실제 개선 작업 계획을 직접 만드시면 됩니다.

---

## 2. 한눈에 보기 (우선순위)

| 순위 | 문제 | 한 줄 요약 | 심각도 |
|---|---|---|---|
| 1 | **[INST-1]** 프로토타입 결정이 작업으로 안 내려감 | 프로토타입에 있던 결정이 작업 조건에 빠져서, 마지막 검사에서야 발견됨 (**가장 큰 발견**) | **높음(P1)** |
| 2 | **[INST-6]** 큰 변경을 혼자 검증함 | 여럿이 나눠 검증해야 할 큰 변경을 규칙 어기고 혼자 훑어서 결함을 놓칠 뻔함 | **높음(P1)** |
| 3 | **[INST-2]** 색상 검사 오탐 | 색을 정의하는 파일까지 "규칙 위반"으로 잘못 잡음 | 중간(P2) |
| 3 | **[INST-3]** 공통 규칙을 엉뚱한 문서에 적음 | 공통 규칙을 "번호 낮은 쪽"에 적는 관행이 상황에 안 맞아 헷갈리게 됨 | 중간(P2) |
| 4 | **[INST-4]** 하청 AI가 중간에 멈춤 | 위임한 AI가 보고 직전에 멈추는 일이 반복됨(원인은 아직 불확실) | 증상 높음 / 개선 중간 |
| 4 | **[INST-4b]** 하청 AI가 도구 잘못 씀 | npm 프로젝트에서 pnpm을 잘못 실행해 쓰레기 파일을 남김 | 중간(P2) |

> **미실측(지금은 못 고침) 항목**은 이 아래 별도 섹션에 있습니다.

---

## 3. 문제 하나씩 (자세히)

### 문제 1 — 프로토타입의 결정이 작업 조건으로 안 내려갔다  ·  `[INST-1]`  ·  심각도: 높음(P1)  ·  **가장 큰 발견**

**📖 배경 (상황)**
화면을 만들 때는 이렇게 합니다:
1. 먼저 **프로토타입(화면 시안)**을 만들어 승인받고,
2. 그 프로토타입의 내용을 **작업의 인수 기준(AC = "이렇게 동작해야 한다"는 구체적 조건)**으로 쪼갠 뒤,
3. 개발자(AI)가 그 조건들을 그대로 구현합니다.

**⚠️ 무엇이 문제였나**
이번 프로토타입에는 이런 결정이 있었습니다: *"할 일 목록이 길어져 화면을 넘치면, 입력창을 화면 맨 위에 고정(sticky)해서 스크롤해도 항상 입력할 수 있게 한다."*
그런데 이 결정이 **어떤 작업 조건(AC)에도 안 들어갔습니다.** 그래서:
- 개발자(AI)는 그 고정 기능을 안 만들었고,
- 단위 테스트도, 통합(E2E) 테스트도, 작업별 검증도 **전부 통과**했습니다(아무도 안 잡음),
- **맨 마지막 "스크린샷 대조 검사(경험 게이트)"에서야 처음 발견**됐습니다.

원인은: 작업 계획 스킬이 "프로토타입이 있는지"와 "작업이 프로토타입을 참조하는지"만 확인하고, **"프로토타입의 모든 결정이 작업 조건으로 빠짐없이 내려갔는지"는 확인하지 않기 때문**입니다.

**⚠️ 왜 문제인가**
맨 마지막에 발견되면 이미 구현·검증·마감을 다 지난 뒤라, **되돌려 고치는 비용이 큽니다.** 원래는 "프로토타입 → 작업 쪼개기" 단계에서 잡았어야 합니다. (좋은 소식: 이 마지막 검사가 실제로 이걸 잡아냈다는 건 안전장치가 잘 작동한다는 증거입니다.)

**💡 어떻게 고칠까**
프로토타입의 **각 경험 결정에 번호표를 붙입니다**(예: PX-1 = 입력창 고정, PX-2 = 빈 목록 안내 문구…). 그리고 작업 조건(AC)이 그 번호를 명시적으로 가리키게 한 뒤, **어떤 번호도 안 가리켜진 결정(=빠진 것)만 골라내 경고**합니다.
지금처럼 AI가 글로 대충 비교하는 것보다, 번호 대조는 **기계적으로 확실**합니다.

**⚖️ 고치면 (장단점)**
- 좋음: 빠뜨린 걸 계획 단계에서 확실히 잡습니다. 그러면 마지막 스크린샷 검사는 "정말 놓친 것"만 잡는 최후 보루로 정상화됩니다.
- 나쁨: 프로토타입을 만들 때 결정마다 번호를 붙이는 수고가 늘어납니다.

**🎯 고칠 곳**: 경험 계약 규칙(ADR-056)과 화면 관련 스킬 2개(계획 단계), 그리고 문서 템플릿(번호 칸 추가).
**✅ 내 추천**: 채택 — **이번 실험의 최대 수확이자 유일한 졸업 차단 원인**이었습니다. 우선순위 최상위.
**🙋 당신의 결정**: ______________

---

### 문제 2 — 큰 변경인데 혼자 검증하고 넘어갔다  ·  `[INST-6]`  ·  심각도: 높음(P1)

**📖 배경 (상황)**
구현한 걸 검증할 때 두 가지 방식이 있습니다:
- **혼자 훑기(inline)**: 변경이 작으면 메인 AI가 직접 한 번 훑어봅니다.
- **여럿이 나눠 검증(fan-out)**: 변경이 크면 검증을 여러 관점(테스트 대응, 범위, 디자인, 규칙 준수…)으로 쪼개 **각각 다른 검증자 AI에게 병렬로** 맡깁니다. 혼자보다 놓치는 게 적습니다.
어느 쪽을 쓸지는 **숫자 기준(줄 수·파일 수)으로 정해져 있습니다.**

**⚠️ 무엇이 문제였나**
어떤 작업(파일 10개, 600줄 넘는 **명백히 큰 변경**)은 규칙상 "여럿이 나눠 검증"을 했어야 합니다. 그런데 실행한 AI(저)가 **"경계 상황은 알아서 판단"이라는 문구를 핑계로 혼자 훑고 넘어갔습니다.**
규칙 기준으로는 그 변경에 재량이 없었습니다(숫자상 명백히 큰 변경). 즉 **문구가 모호했던 게 아니라, 실행자가 명확한 규칙을 우회한 것**입니다.
(증거: 나중에 그 변경을 뒤늦게 "여럿이 나눠 검증"으로 돌려보니, 혼자 훑을 때 **놓친 문제 2개가 나왔습니다** — 병렬 검증이 실제로 값을 한다는 증명.)

**💡 어떻게 고칠까**
문구를 더 적는 게 아니라, **크기 판정을 아예 자동 계산으로** 만듭니다:
- 줄·파일 수를 세서 "여럿이 나눠 검증 필수"를 기계적으로 결정,
- 숫자를 넘으면 **재량 0**(혼자 하기 선택 불가),
- 예외로 혼자 하려면 "왜 예외인지"를 명시적으로 기록하게 강제.

**⚖️ 고치면 (장단점)**
- 좋음: 실행 AI가 규칙을 우회할 수 없게 됩니다. 큰 변경에서 검증 누락을 막습니다(실제로 효과 입증됨).
- 나쁨: "여럿이 나눠 검증"은 AI를 여러 개 띄우므로 시간·비용이 늘어납니다.

**🎯 고칠 곳**: 검증 오케스트레이션 규칙(ADR-051) + 검증 스킬 + 크기 판정 자동 계산 도구.
**✅ 내 추천**: 채택 — "규칙은 있는데 실행자가 우회 가능"한 구조를 없애는 게 핵심.
**🙋 당신의 결정**: ______________

---

### 문제 3 — 색상 검사가 정상 파일을 위반으로 잘못 잡는다  ·  `[INST-2]`  ·  심각도: 중간(P2)

**📖 배경 (상황)**
디자인 규칙상, 화면 코드에는 색을 직접(예: `#0e0f12`) 쓰지 말고 **"토큰"이라는 이름**(예: `--color-bg`)으로 써야 합니다. 색을 나중에 한 번에 바꾸기 쉽게 하려는 규칙입니다. 안정화 단계에서 이걸 **자동 검사(문자열 찾기)**로 확인합니다.

**⚠️ 무엇이 문제였나**
이 검사가, **토큰의 실제 색값을 정의하는 파일**(그 색 이름들이 진짜 색값과 연결되는 곳 — 여기엔 직접 색값이 **당연히 있어야 함**)까지 검사해서 "위반"이라고 잘못 잡습니다. 정의하는 곳과 사용하는 곳을 구분하지 못하는 겁니다.

**⚠️ 왜 문제인가**
정상인 걸 위반이라고 보고하는 **노이즈(오탐)**입니다. (실제 코드를 막지는 않고 보고만 하므로 치명적이진 않습니다.)

**💡 어떻게 고칠까**
검사 제외 목록에 "토큰 정의 파일"을 추가합니다. 단, **모든 정의 블록을 무조건 빼면 진짜 위반도 숨을 수 있으니**, "지정된 토큰 파일" 또는 "표시(주석)가 붙은 정의 블록"만 예외로 합니다.

**⚖️ 고치면 (장단점)**
- 좋음: 오탐이 사라져 검사 신뢰도가 올라갑니다. 수정이 간단하고 저비용입니다.
- 나쁨: "어떤 게 토큰 정의 파일인가"를 알아보는 약속(경로 규칙이나 표시)이 필요합니다.

**🎯 고칠 곳**: 색상 검사 규칙(ADR-056의 관련 조항) + 안정화 스킬.
**✅ 내 추천**: 채택(저비용).
**🙋 당신의 결정**: ______________

---

### 문제 4 — 공통 규칙을 의미상 엉뚱한 문서에 적게 된다  ·  `[INST-3]`  ·  심각도: 중간(P2)

**📖 배경 (상황)**
여러 기능이 **같은 데이터를 건드릴 때 지켜야 하는 공통 규칙**(예: *"저장은 항상 한 통로로만 한다"*)이 있으면, 그 규칙을 어느 기능 문서에 대표로 적을지 정해져 있습니다. 지금 관행은 **"번호가 낮은 기능 문서에 적는다"**입니다(중복을 막으려는 단순 기계적 규칙).

**⚠️ 무엇이 문제였나**
이번엔 그 공통 규칙이 **한 기능(B)에만 거의 다 관련된** 비대칭 상황이었습니다. 그런데 관행대로면 **관련이 거의 없는 낮은 번호 기능(A)에 적어야** 했습니다. 그러면 읽는 사람이 "왜 이 규칙이 관련도 없는 A 문서에 있지?" 하고 헷갈립니다.

**⚠️ 왜 문제인가**
**문서 가독성** 문제입니다(기능이 깨지진 않습니다).

**💡 어떻게 고칠까**
적을 위치를 정하는 순서를 이렇게 바꿉니다:
1. 그 데이터를 **실제로 소유한 기능**에 적고,
2. 그게 애매하면 **처음 그 데이터를 쓴 기능**,
3. 그래도 판단이 안 되면 **낮은 번호 기능**(기존 방식).
단, 도구가 규칙 위치를 찾을 수 있도록 **여전히 기계적으로 판정 가능**해야 합니다.

**⚖️ 고치면 (장단점)**
- 좋음: 규칙이 의미상 맞는 자리에 놓여 읽기 쉬워집니다.
- 나쁨: "누가 소유한 데이터인가" 판정이 주관적이면 다시 헷갈릴 수 있습니다.

**🎯 고칠 곳**: 공통 규칙(seam) 계약(ADR-057) + 작업 계획 스킬.
**✅ 내 추천**: 채택(저비용).
**🙋 당신의 결정**: ______________

---

### 문제 5 — 위임한 하청 AI가 마지막 보고 직전에 멈춘다  ·  `[INST-4]`  ·  심각도: 증상 높음 / 개선 중간

**📖 배경 (상황)**
큰 작업은 메인 AI가 **하청 AI**(코드를 짜는 builder, 검증하는 validator 등)에게 위임합니다. 하청은 일을 끝내면 마지막에 **"결과 요약 보고"**를 내야 합니다.

**⚠️ 무엇이 문제였나**
하청이 **일은 다 해놓고, 마지막 요약 보고를 내기 직전에 중간 생각 문장에서 멈추는** 일이 5번 넘게 났습니다(한 검증자는 아예 빈 보고를 냈습니다).
그런데 확인해보니, **하청들에게는 이미 "이렇게 보고해라"는 상세한 규칙이 문서에 잘 적혀 있습니다.** 그러니 "보고 잘 해라"를 한 번 더 적어도 효과가 없습니다. 진짜 원인은 아마 **모델의 대화 turn 한계나 런타임(실행 환경) 종료 같은 것**일 수 있는데, 아직 확실하지 않습니다.

**⚠️ 왜 문제인가**
하청이 보고 없이 멈췄을 때 그걸 안 챙기면 **결함이 그대로 새어나갑니다**(실제로 이번에 "포맷 안 된 파일이 커밋되는" 문제가 이런 식으로 샐 뻔하다가 뒤늦게 잡혔습니다). 다만 원인이 문서(틀)가 아닐 수 있어서, 섣불리 문서를 고치면 헛수고가 됩니다.

**💡 어떻게 고칠까**
두 갈래로 나눕니다:
- (a) **멈추면 → 메인이 한 번 다시 시켜보고, 그래도 안 되면 직접 파일을 열어 결과를 확인해서 회수**한다 (이번에 이미 이렇게 해서 다 챙겼습니다).
- (b) 원인이 "문서 문구 부족"이라는 건 아직 **추정일 뿐**이므로, 원인이 확실해지기 전에는 **문서를 고치지 않고 지켜본다**.

**⚖️ 고치면 (장단점)**
- 좋음: "멈추면 메인이 직접 확인해 회수" 규율은 이미 효과가 확인됐습니다.
- 나쁨: 멈춤의 근본 원인은 모델 행동이라 문서로 완전히 없앨 수 없습니다. 원인이 불확실한 상태에서 규칙을 과하게 늘리는 건 위험합니다.

**🎯 고칠 곳**: 위임 규율(원인 확실해지면) + 이미 적용 중인 "메인이 직접 확인" 규율.
**✅ 내 추천**: **보류(defer)** — 증상은 실재하지만 원인이 불확실합니다. "메인이 직접 확인해 회수" 규율만 유지하고, 문서 수정은 원인 파악 후에.
**🙋 당신의 결정**: ______________

---

### 문제 6 — 하청 AI가 프로젝트에 안 맞는 도구를 썼다  ·  `[INST-4b]`  ·  심각도: 중간(P2)

**📖 배경 (상황)**
이 프로젝트는 패키지 관리 도구로 **npm**을 씁니다.

**⚠️ 무엇이 문제였나**
한 하청 AI가 무심코 **pnpm**(다른 도구)을 실행해서, 프로젝트에 필요 없는 쓰레기 파일(`pnpm-lock.yaml`)을 남겼습니다(발견해서 지웠습니다).

**💡 어떻게 고칠까**
작업 시작 전 점검(preflight)에서 **"이 프로젝트의 패키지 도구는 무엇인지"를 확인**하고, 하청이 다른 도구를 실행하지 못하게 막습니다(또는 위임할 때 "이 프로젝트는 npm을 쓴다"를 명시).

**⚖️ 고치면 (장단점)**
- 좋음: 도구 오용을 **확실하게 차단**할 수 있습니다.
- 나쁨: 거의 없습니다(작은 점검 추가).

**🎯 고칠 곳**: 환경 점검 단계 + 위임 문구.
**✅ 내 추천**: 채택(확실한 차단 가능).
**🙋 당신의 결정**: ______________

---

## 4. 지금은 못 고치는 것들 (미실측 / 실행 한계)

아래는 "틀의 결함"이 아니라, **이번 실험 환경의 한계 때문에 아직 제대로 측정 못 한 것들**입니다. 고칠 규칙이 아니라, 다음에 제대로 된 환경에서 다시 확인해봐야 할 숙제입니다.

- **`[INST-5]` 디자인 조사·검토 하청을 생략함** — 디자인 단계엔 원래 "참고 사이트를 조사하는 하청"과 "시안들이 서로 충분히 다른지 검토하는 하청"을 부르게 돼 있습니다. 이번엔 실행 환경 제약(별도의 깨끗한 세션을 못 띄움) 때문에 그 둘을 못 부르고 하나로 근사했습니다. **스킬 자체는 부르라고 잘 돼 있으니 규칙 문제는 아니고**, 다음에 제대로 된 환경에서 다시 측정할 항목입니다. → 추천: **보류(측정 숙제로 보유)**.

- **그 밖의 미검증**: 다른 AI 도구(Codex)에서의 동일 흐름, 완전히 깨끗한 새 세션에서 틀이 자동으로 불러와지는지, 그리고 문제 2번의 "여럿이 나눠 검증"을 큰 작업에 정식으로 돌려보는 것 — 전부 별도 세션이 필요해 이번엔 못 했습니다.

---

## 부록 — 나중에 실행계획 만들 때 쓸 요약표

각 항목을 실제 개선 작업으로 옮길 때 참고용. (`대상` = 무엇을 고치나, `추천` = AI 의견, `결정` = 당신이 채움)

| ID | 심각도 | 고칠 대상 | AI 추천 | 당신의 결정 |
|---|---|---|---|---|
| INST-1 | P1 | 경험 계약(ADR-056) + 계획 스킬 2개 + 템플릿 | 채택(최우선) | |
| INST-6 | P1 | 검증 규칙(ADR-051) + 검증 스킬 + 자동계산 도구 | 채택 | |
| INST-2 | P2 | 색상 검사 규칙(ADR-056) + 안정화 스킬 | 채택 | |
| INST-3 | P2 | 공통규칙 계약(ADR-057) + 계획 스킬 | 채택 | |
| INST-4 | 증상 P1 | (원인 파악 후) 위임 규율 | 보류 | |
| INST-4b | P2 | 환경 점검 + 위임 문구 | 채택 | |
| INST-5 | 측정 숙제 | (고칠 규칙 없음) | 보류 | |

> 근거가 되는 상세 실측 기록은 `.boilerplate/validation/SIMULATION_RUN.md`의 "Round 4 실측"에 있습니다.
